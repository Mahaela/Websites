import { ROUTES } from "@angular/router";
import { UserDetailComponent } from './user.
export const USER_ROUTES: Routes = [
{ path: 'detail', component: UserDetailComponent },
{ path: 'edit', component: UserEditComponent } ];